include "sample-common.conf"
fusion.jdbc.default {
  poolName = "sample"
  jdbcUrl = "jdbc:h2:mem:sample;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE"
  username = "root"
  password = "123qwe!@#"
  connectionTestQuery = "select 1;"
  maximumPoolSize = 2
}
