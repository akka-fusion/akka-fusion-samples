include "sample-common.conf"
fusion.discovery {
  enable = true
  nacos {
    #serverAddr =
    #namespace =
    #dataId =
    group = "DEFAULT_GROUP"
    timeoutMs = 3000
    #serviceName =
    #autoRegisterInstance = true
  }
}
